"use client";import{useEffect,useState}from"react";const A=process.env.NEXT_PUBLIC_API_URL||"http://localhost:5000/api";export default function Page(){const[d,s]=useState<any>();useEffect(()=>{const t=localStorage.getItem("token");fetch(`${A}/platform-tenancy/enterprise/architecture`,{headers:t?{Authorization:`Bearer ${t}`}:{}}).then(r=>r.json()).then(x=>s(x.data))},[]);return <main className="ds-page"><h1>Enterprise SaaS Hierarchy</h1><p>Platform → Organization → Business Unit → Tenant → Store → Department</p><pre>{JSON.stringify(d,null,2)}</pre></main>}