export type EnterpriseArtifact={path:string;content:string;contentType:string};
type Input={templateKey:string;analysis:any;sourceEntries:Array<[string,string]>};
function laravel(entries:Array<[string,string]>) {const routes:any[]=[];const controllers:string[]=[];const models:string[]=[];const requests:string[]=[];const policies:string[]=[];const migrations:string[]=[];const unsupported:string[]=[];for(const [path,source] of entries){const p=path.replace(/\\/g,"/");if(!/\.(php|blade\.php)$/i.test(p))continue;if(/routes\/(web|api)\.php$/i.test(p)){const r=/Route::(get|post|put|patch|delete|resource)\s*\(\s*["']([^"']+)["']\s*,\s*([^)]+)\)/g;let m;while((m=r.exec(source)))routes.push({method:m[1].toUpperCase(),path:m[2],handler:m[3].trim()});if(/Route::group|middleware\s*\(|domain\s*\(/.test(source))unsupported.push(p+": grouped route/middleware review");}if(/app\/Http\/Controllers\//i.test(p)){controllers.push(p);if(/DB::transaction|dispatch\s*\(|event\s*\(|queue\s*\(/.test(source))unsupported.push(p+": transaction/event/queue review");}if(/app\/Models\//i.test(p))models.push(p);if(/app\/Http\/Requests\//i.test(p))requests.push(p);if(/app\/Policies\//i.test(p))policies.push(p);if(/database\/migrations\//i.test(p))migrations.push(p);}return{detected:routes.length+controllers.length+models.length+migrations.length>0,routes,controllers,models,requests,policies,migrations,unsupported};}
const capture=`#!/usr/bin/env node\nimport{spawn}from"node:child_process";import{mkdir,writeFile}from"node:fs/promises";import path from"node:path";const[source,target,out="./certification-output"]=process.argv.slice(2);if(!source||!target)process.exit(2);const chrome=process.env.CHROME_PATH||"C:/Program Files/Google/Chrome/Application/chrome.exe";const port=9333+Math.floor(Math.random()*500),proc=spawn(chrome,["--headless=new","--disable-gpu",\`--remote-debugging-port=\${port}\`,\`--user-data-dir=\${path.resolve(out,".chrome")}\`],{stdio:"ignore"});const sleep=m=>new Promise(r=>setTimeout(r,m));async function ws(url){return new Promise((r,j)=>{const s=new WebSocket(url);s.onopen=()=>r(s);s.onerror=j})}async function run(browser,url,label,w,h){const s=await ws(browser);let id=0;const q=new Map;s.onmessage=e=>{const m=JSON.parse(e.data);if(q.has(m.id)){q.get(m.id)(m);q.delete(m.id)}};const call=(method,params={},sessionId)=>new Promise((r,j)=>{const i=++id;q.set(i,m=>m.error?j(Error(m.error.message)):r(m.result||{}));s.send(JSON.stringify({id:i,method,params,...(sessionId?{sessionId}:{})}))});const t=await call("Target.createTarget",{url:"about:blank"}),a=await call("Target.attachToTarget",{targetId:t.targetId,flatten:true}),sid=a.sessionId;await call("Emulation.setDeviceMetricsOverride",{width:w,height:h,deviceScaleFactor:1,mobile:w<=768},sid);await call("Page.enable",{},sid);await call("Runtime.enable",{},sid);await call("Page.navigate",{url},sid);await sleep(2200);const expr=\`(()=>{const keys=["display","position","visibility","opacity","color","backgroundColor","fontFamily","fontSize","fontWeight","lineHeight","letterSpacing","marginTop","marginRight","marginBottom","marginLeft","paddingTop","paddingRight","paddingBottom","paddingLeft","borderRadius","width","height","gridTemplateColumns","gap","transform","boxShadow"];const nodes=[...document.querySelectorAll("body *")].filter(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>0&&r.height>0&&s.display!=="none"&&s.visibility!=="hidden"}).slice(0,5000).map(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return{tag:e.tagName.toLowerCase(),id:e.id||"",className:typeof e.className==="string"?e.className:"",text:(e.textContent||"").trim().replace(/\\s+/g," ").slice(0,180),rect:{x:r.x,y:r.y,width:r.width,height:r.height},styles:Object.fromEntries(keys.map(k=>[k,s[k]]))}});return{viewport:{width:innerWidth,height:innerHeight},document:{scrollWidth:document.documentElement.scrollWidth,scrollHeight:document.documentElement.scrollHeight},nodes}})()\`;const snap=await call("Runtime.evaluate",{expression:expr,returnByValue:true},sid),png=await call("Page.captureScreenshot",{format:"png",captureBeyondViewport:true},sid);await mkdir(out,{recursive:true});const p=path.join(out,\`\${label}-\${w}x\${h}\`);await writeFile(p+".json",JSON.stringify(snap.result.value,null,2));await writeFile(p+".png",Buffer.from(png.data,"base64"));await call("Target.closeTarget",{targetId:t.targetId});s.close()}try{let browser;for(let i=0;i<30;i++){try{const r=await fetch(\`http://127.0.0.1:\${port}/json/version\`);if(r.ok){browser=(await r.json()).webSocketDebuggerUrl;break}}catch{}await sleep(250)}for(const w of[320,360,390,768,1024,1366,1440,1920]){await run(browser,source,"source",w,900);await run(browser,target,"migrated",w,900)}}finally{proc.kill()}\n`;
const compare=`#!/usr/bin/env node\nimport{readFile,readdir,writeFile}from"node:fs/promises";import path from"node:path";const[d="./certification-output"]=process.argv.slice(2),files=await readdir(d),results=[];const key=n=>[n.tag,n.id,n.className,n.text].join("|");for(const sf of files.filter(f=>f.startsWith("source-")&&f.endsWith(".json"))){const mf="migrated-"+sf.slice(7);if(!files.includes(mf))continue;const a=JSON.parse(await readFile(path.join(d,sf))),b=JSON.parse(await readFile(path.join(d,mf))),map=new Map(b.nodes.map(n=>[key(n),n]));let matched=0,styles=0,total=0,geo=0;for(const n of a.nodes){const t=map.get(key(n));if(!t)continue;matched++;if(["x","y","width","height"].every(k=>Math.abs(n.rect[k]-t.rect[k])<=2))geo++;for(const k of Object.keys(n.styles)){total++;if(n.styles[k]===t.styles[k])styles++}}results.push({viewport:a.viewport,domScore:a.nodes.length?matched/a.nodes.length*100:0,geometryScore:matched?geo/matched*100:0,computedStyleScore:total?styles/total*100:0,horizontalOverflow:b.document.scrollWidth>b.viewport.width+2})}const status=results.length&&results.every(x=>x.domScore>=95&&x.geometryScore>=92&&x.computedStyleScore>=95&&!x.horizontalOverflow)?"PASS":"BLOCKED",out={generatedAt:new Date().toISOString(),results,status};await writeFile(path.join(d,"dom-computed-style-result.json"),JSON.stringify(out,null,2));if(status!=="PASS")process.exit(1);\n`;
const pixels=`param([Parameter(Mandatory=$true)][string]$Directory,[double]$MinimumScore=95)\nAdd-Type -AssemblyName System.Drawing;$R=@();Get-ChildItem $Directory -Filter 'source-*.png'|%{$a=[Drawing.Bitmap]::FromFile($_.FullName);$p=Join-Path $Directory ('migrated-'+$_.Name.Substring(7));if(!(Test-Path $p)){$a.Dispose();return};$b=[Drawing.Bitmap]::FromFile($p);$diff=0L;$total=[int64]$a.Width*$a.Height;if($a.Width-ne$b.Width-or$a.Height-ne$b.Height){$score=0}else{for($y=0;$y-lt$a.Height;$y++){for($x=0;$x-lt$a.Width;$x++){$c=$a.GetPixel($x,$y);$d=$b.GetPixel($x,$y);if(([Math]::Abs($c.R-$d.R)+[Math]::Abs($c.G-$d.G)+[Math]::Abs($c.B-$d.B))-gt16){$diff++}}};$score=[Math]::Round((1-$diff/[double]$total)*100,4)};$R+=[pscustomobject]@{viewport=$_.BaseName.Substring(7);score=$score;status=if($score-ge$MinimumScore){'PASS'}else{'BLOCKED'}};$a.Dispose();$b.Dispose()};$O=[pscustomobject]@{results=$R;status=if($R.Count-and-not($R.status-contains'BLOCKED')){'PASS'}else{'BLOCKED'}};$O|ConvertTo-Json -Depth 8|Set-Content (Join-Path $Directory 'pixel-parity-result.json');if($O.status-ne'PASS'){exit 1}\n`;
const e2e=`param([Parameter(Mandatory=$true)][string]$SourceUrl,[Parameter(Mandatory=$true)][string]$MigratedUrl,[string]$OutputDirectory='.\\certification-output')\n$R=Split-Path -Parent $MyInvocation.MyCommand.Path;& node (Join-Path $R 'capture-dom-style.mjs') $SourceUrl $MigratedUrl $OutputDirectory;if($LASTEXITCODE){throw 'Capture failed'};& node (Join-Path $R 'compare-dom-style.mjs') $OutputDirectory;$dom=$LASTEXITCODE;& powershell -NoProfile -ExecutionPolicy Bypass -File (Join-Path $R 'compare-images.ps1') -Directory $OutputDirectory;$pixel=$LASTEXITCODE;$o=[pscustomobject]@{generatedAt=(Get-Date).ToString('o');domStatus=$dom;pixelStatus=$pixel;status=if($dom-eq0-and$pixel-eq0){'PASS'}else{'BLOCKED'}};$o|ConvertTo-Json|Set-Content (Join-Path $OutputDirectory 'enterprise-certificate.json');if($o.status-ne'PASS'){exit 1}\n`;
export function buildEnterpriseArtifacts(i:Input):EnterpriseArtifact[]{const l=laravel(i.sourceEntries),base=`src/templates/imported/${i.templateKey}/certification`,cfg={engineVersion:"2026.30.0",thresholds:{pixel:95,dom:95,computedStyle:95,geometry:92},viewports:[320,360,390,768,1024,1366,1440,1920],apiPolicy:"KEEP_DESIGN_REPLACE_DEMO_DATA_WITH_REAL_API",adminPolicy:"FULL_CRUD_TENANT_STORE_SCOPED",aiPolicy:"LIVE_PROVIDER_MODEL_ROUTE_RECONCILIATION",laravel:l,publishGate:"FAIL_CLOSED"};return[{path:`${base}/capture-dom-style.mjs`,content:capture,contentType:"text/javascript"},{path:`${base}/compare-dom-style.mjs`,content:compare,contentType:"text/javascript"},{path:`${base}/compare-images.ps1`,content:pixels,contentType:"text/plain"},{path:`${base}/RUN_END_TO_END_CERTIFICATION.ps1`,content:e2e,contentType:"text/plain"},{path:`${base}/enterprise-certification.config.json`,content:JSON.stringify(cfg,null,2),contentType:"application/json"},{path:`${base}/laravel-contract-map.json`,content:JSON.stringify(l,null,2),contentType:"application/json"},{path:`${base}/publish-gate.json`,content:JSON.stringify({status:"AWAITING_RUNTIME_CERTIFICATION",publishReady:false,required:["build","real-api","tenant-isolation","pixel","dom","computed-style","admin-crud","ai-reconciliation","laravel-supported"]},null,2),contentType:"application/json"}]}
